.vscode
carvel-artifacts/
ansible-supervisor.yml
config/crd.yml
/controller/ansible-supervisor-service
config/config-release.yml
# Built by test/e2e.sh, and by anyone running `go build` in there.
/test/fakeawx/fakeawx

# The live gate's settings, including an AWX token and a vCenter
# password. .env.example is the committed template.
.env

# Extracted from CHANGELOG.md by `make release-notes` in the release workflow.
/release-notes.md
